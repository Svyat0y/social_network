import React from 'react';
import classes from './Header.module.css';

const Header = () => {
    return (
        <header className={classes.header}>
            <h1>Welcome to the best social network ih the world ;)</h1>
        </header>
    );
}

export default Header;